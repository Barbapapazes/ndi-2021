@layout('layouts/main')
@set('title', 'Editer la page d\'un bateau')

@section('main')
<form action="{{ route('BoatsPagesController.store', {boat_id: boat_id}) }}" method="post">
  <label for="content">Contenu</label>
  <textarea name="content" id="content" cols="30" rows="10" >{{ page.content }}</textarea>


<button type="submit">Envoyer pour l'édition</button>
</form>
@end
