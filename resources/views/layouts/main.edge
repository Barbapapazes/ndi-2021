<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>{{ title }} - team?.name</title>
  @entryPointStyles('app')
</head>
<body>
  <canvas id="ThreeCanvas" class="w-full h-full block inset-0 z-0 bg-black"></canvas>
  <div class="absolute inset-0 w-full h-full">
    <header class="xl:flex flex-row justify-between items-center px-8 absolute w-full h-20 left-0 top-0 border-b-2 border-white hidden">
       <div class="flex flex-row">
        <img src="favicon.svg" alt="Vite App" class="w-14" />
        <h1 class="text-4xl font-bold text-white leading-normal px-4">Sauveteurs du dunkerquois</h1>
      </div>
      <div class="flex flex-row justify-center items-center">
        @if(auth.isLoggedIn)
        <form action="{{ route('AuthController.logout') }}" method="post">
          <button type="submit" class="text-xl font-bold text-primary bg-white leading-normal w-32 h-12 rounded-lg m-8">Se déconnecter</button>
        </form>
        @else
        <a href="{{ route('AuthController.login') }}" class="text-xl font-bold text-primary bg-white leading-normal w-32 h-12 rounded-lg m-8">
          Se connecter
        </a>
        <a href="{{ route('AuthController.signup') }}" class="text-xl font-bold text-primary bg-white leading-normal w-32 h-12 rounded-lg m-8">
          S'incrire
        </a>
        @end
      </div>
    </header>
    <nav>
      <ul>
        <li>
          <a href="{{ route('PersonsController.index') }}">
            Sauveteurs
          </a>
        </li>
        <li>
          <a href="{{ route('ExcursionsController.index') }}">
            Sorties en mer
          </a>
        </li>
        <li>
          <a href="{{ route('StationsController.index') }}">
            Stations
          </a>
        </li>
        <li>
          <a href="{{ route('ServicesController.index') }}">
            Services des ports
          </a>
        </li>
      </ul>
    </nav>
    <main>
      @!section('main')
    </main>
  </div>
  @entryPointScripts('app')
</body>
</html>
