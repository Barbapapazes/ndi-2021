@layout('layouts/main')
@set('title', 'Editer la page d\'une personne')

@section('main')
<form action="{{ route('PersonsPagesController.store', {person_id: person_id}) }}" method="post">
  <label for="content">Contenu</label>
<textarea name="content" id="content" cols="30" rows="10" ></textarea>


<button type="submit">Envoyer pour l'édition</button>
</form>
@end
